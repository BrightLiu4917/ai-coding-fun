# 测试用例

> 本文件在 OpenSpec 设计阶段由测试工程师（`agent-test`）产出，随 change 一起等待用户确认。
> 用例是验收契约：实现必须向用例收敛，执行阶段禁止为迁就实现而修改已确认用例；确需修改时必须重新确认。
> 状态由测试工程师在实现完成后回填；发布审查前不得残留“已设计”。

## 用例清单

| 用例ID | 关联场景 | 类型 | 前置条件 | 步骤 | 预期结果 | 验证方式 | 状态 |
|--------|----------|------|----------|------|----------|----------|------|
| TC-01 | 填写 spec.md 中的场景名称 | 正常流 | | | | 单测 | 已设计 |
| TC-02 | | 异常流 | | | | 集成 | 已设计 |
| TC-03 | | 权限 | | | | E2E | 已设计 |
| TC-04 | | 边界 | | | | 手动 | 已设计 |

- 类型：正常流 / 异常流 / 权限 / 边界 / 兼容性 / 并发
- 验证方式：单测 / 集成 / E2E / 手动
- 状态：已设计 / 通过 / 失败

## 自动回填约定

- 自动化测试的方法名或 describe 名必须包含用例ID（如 `test_TC01_分页查询`、`TC-02 非法参数`）。
- 运行 `OPENSPEC_CHANGE_ID=<change-id> bash .ai-control/control/scripts/run-tests.sh` 后，状态列会按 JUnit 报告自动回填为 通过/失败。
- 验证方式为“手动”的用例不自动回填，由测试工程师执行手动步骤后人工回填。

## 覆盖要求

- 每个 spec 场景至少一条用例。
- 涉及 API（affected_apis 非 none）时，必须包含异常流用例：参数错误、数据不存在、状态不允许。
- 涉及页面（affected_pages 非 none）时，必须包含权限态、空态、错误态用例。
- 涉及写操作时，必须包含重复提交或并发用例。

## 测试命令

```bash
# 填写本 change 的测试命令，例如 bash .ai-control/control/scripts/run-tests.sh
```

## 手动验证步骤

- 步骤：

## 残余风险

- 风险：
